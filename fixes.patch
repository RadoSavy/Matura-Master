diff --git a/public/scripts/auth.js b/public/scripts/auth.js
--- a/public/scripts/auth.js
+++ b/public/scripts/auth.js
@@ -25,1 +25,1 @@
-  return {key, salt: Array.from(salt)};
+  return {key: key, salt: Array.from(salt)};
diff --git a/src/services/firebase.js b/src/services/firebase.js
--- a/src/services/firebase.js
+++ b/src/services/firebase.js
@@ -27,1 +27,1 @@
-export const logout = () => signOut(auth);
+export const logout = function() { return signOut(auth) };
diff --git a/src/pages/Login.jsx b/src/pages/Login.jsx
--- a/src/pages/Login.jsx
+++ b/src/pages/Login.jsx
@@ -18,1 +18,1 @@
-              Authorization: `Bearer ${tokenResponse.access_token}`,
+              Authorization: "Bearer "+tokenResponse.access_token,
@@ -41,1 +41,1 @@
-      {error && <p style={{ color: 'red', marginBottom: '10px' }}>{error}</p>}
+      {error ? <p style={{ color: 'red', marginBottom: '10px' }}>{error}</p> : null}
@@ -43,1 +43,1 @@
-        onClick={() => login()}
+        onClick={function() { return login() }}
